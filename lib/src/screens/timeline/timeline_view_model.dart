import 'package:injectable/injectable.dart';
import '../base/base_view_model.dart';

@injectable
class TimelineViewModel extends BaseViewModel {
  String testTitle = 'TimelineView'; //TODO remove, added just for testing
}